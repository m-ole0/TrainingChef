<div class="container">
  <div class="row mx-auto">
    <div class="col-12 mx-auto text-center mt-5">
      <h4>タグ名</h4>
      <h3>"<%= @tag.name %>"</h3>
      <p class="mt-4">"<%= @tag.name %>"に関連する投稿<%= @tag.recipes.count %>件</p>
      <h4 class="mt-5">本当にタグを削除しますか？</h4>
      <p class="mt-3">タグを削除すると、レシピからも該当のタグが削除されます。</p>
      <%= link_to "タグ一覧ページへ戻る", admin_tags_path, class:"btn btn-success" %>
      <%= link_to "タグを削除する", admin_tag_path(@tag), method: :delete, class:"btn btn-danger" %>
    </div>
  </div>
</div>