<div class="container">
  <div class="row">
    <div class="col-12 col-lg-8 mx-auto mt-5">
      <% if @users.present? %>
        <h4 class="text-center"><%= @user.name %>さんをフォロワーしているユーザー一覧</h4>
        <div class="text-center">
          <%= render "public/users/index", users: @users %>
        </div>
      <% else %>
        <h4 class="text-center"><%= @user.name %>さんはまだフォローされていません</h4>
      <% end %>
    </div>
  </div>
</div>